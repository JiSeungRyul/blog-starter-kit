---
description: "에이전트 최적화 (GEO/AIEO/AEO) 작업 목록"
---

# 작업: 에이전트 최적화 (GEO/AIEO/AEO)

**입력**: `/specs/001-agent-optimization/`의 설계 문서
**전제 조건**: plan.md, spec.md, data-model.md

**테스트**: `quickstart.md`에 정의된 수동 검증 단계.

## 1단계: 설정 (공유 인프라)

**목적**: 프로젝트 초기화 및 기본 구조

- [ ] T001 프로젝트 구조 확인 및 `src/lib` 존재 여부 확인

## 2단계: 기초 (차단 전제 조건)

**목적**: 모든 사용자 스토리를 구현하기 전에 완료해야 하는 핵심 인프라

- [ ] T002 데이터 모델에서 TypeScript 인터페이스(`JsonLdWebSite`, `JsonLdArticle`)를 사용하여 `src/lib/json-ld.ts` 생성

## 3단계: 사용자 스토리 1 - AI 검색 엔진 인덱싱 (우선순위: P1) 🎯 MVP

**목표**: 블로그 게시물과 홈페이지가 JSON-LD를 사용하여 AI 검색 엔진에 의해 올바르게 인덱싱되도록 보장합니다.

**독립 테스트**: 페이지 소스 또는 Google Rich Results Test를 사용하여 JSON-LD 출력을 확인합니다.

### 사용자 스토리 1 구현

- [ ] T003 [P] [US1] `src/lib/json-ld.ts`에 `generateWebSiteSchema` 헬퍼 구현
- [ ] T004 [P] [US1] `src/lib/json-ld.ts`에 `generateArticleSchema` 헬퍼 구현
- [ ] T005 [US1] `src/app/page.tsx`에 `WebSite` JSON-LD 주입
- [ ] T006 [US1] `src/app/posts/[slug]/page.tsx`에 `Article` JSON-LD 주입

**체크포인트**: 홈페이지와 게시물 페이지에 유효한 JSON-LD 스크립트 태그가 포함되어 있습니다.

## 4단계: 사용자 스토리 2 - llms.txt를 통한 LLM 에이전트 접근 (우선순위: P2)

**목표**: AI 에이전트가 사이트 콘텐츠를 효율적으로 소비할 수 있도록 표준화된 텍스트 파일을 제공합니다.

**독립 테스트**: `/llms.txt`에 접근하여 콘텐츠 형식을 확인합니다.

### 사용자 스토리 2 구현

- [ ] T007 [US2] 라우트 핸들러 파일 `src/app/llms.txt/route.ts` 생성
- [ ] T008 [US2] 모든 게시물을 가져와 `text/plain`으로 포맷하는 `GET` 핸들러 구현
- [ ] T009 [US2] 응답에 사이트 제목, 설명, URL이 포함된 게시물 목록이 포함되어 있는지 확인

**체크포인트**: `/llms.txt`에 접근 가능하고 올바르게 포맷되어 있습니다.

## 최종 단계: 다듬기 및 공통 관심사

- [ ] T010 AI 사용자 에이전트를 허용하도록 `robots.txt` 확인 및 구성 (`src/app/robots.ts` 또는 정적 파일 사용)

## 의존성

- US1 (JSON-LD)은 T002 (인터페이스)에 의존합니다.
- US2 (llms.txt)는 US1과 독립적이지만 `lib/api.ts` 사용을 공유합니다.

## 구현 전략

1.  **기초**: 타입 안전성을 보장하기 위해 타입을 먼저 정의합니다.
2.  **MVP (US1)**: JSON-LD 헬퍼를 구현하고 주입합니다. 이는 즉시 SEO/GEO를 개선합니다.
3.  **확장 (US2)**: `llms.txt` 라우트 핸들러를 추가합니다.
